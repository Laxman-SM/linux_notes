#yum check
	yum provides ifconfig

#networking applications to install
	yum install net-tools
	yum install iproute

#editors to install
	yum install nano

#time services
	yum install ntp
	sudo systemctl start ntpd
	sudo systemctl enable ntpd